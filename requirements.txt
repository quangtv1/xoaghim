# Core dependencies
PyQt5>=5.15.0
PyMuPDF>=1.20.0
opencv-python>=4.5.0
numpy>=1.20.0
Pillow>=9.0.0

# Geometry processing (for text protection)
shapely>=2.0.0

# AI Model dependencies (for text protection feature)
# Layout Parser (Detectron2) - recommended
# pip install layoutparser torchvision
# pip install "detectron2@git+https://github.com/facebookresearch/detectron2.git@v0.5#egg=detectron2"

# Alternative: PaddleOCR (if Detectron2 installation fails)
# pip install paddlepaddle paddleocr
